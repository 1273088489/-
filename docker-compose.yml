services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: quanzhan
      POSTGRES_PASSWORD: quanzhan
      POSTGRES_DB: quanzhan
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
